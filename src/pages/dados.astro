---
import Layout from "../layouts/Layout.astro";
import Navbar from "../components/Navbar.astro";
import Footer from "../components/Footer.astro";
---

<Layout>
    <Navbar />

<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>AlphaBioma - Análise de Dados</title>
  <style>
    :root {
      --primary: #4B9AFB;
      --primary-dark: #3B7FD1;
      --secondary: #979df0;
      --dark: #0F172A;
      --light: #F8FAFC;
      --accent: #1a24a7;
    }
    
    body {
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      background: linear-gradient(135deg, var(--dark), #060d19);
      color: var(--light);
      overflow-x: hidden;
      margin: 0;
      padding: 0;
    }
    
    .section {
      padding: 6rem 1.5rem;
      position: relative;
    }
    
    .container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 1.5rem;
    }
    
    /* Hero */
    .hero-section {
      position: relative;
      min-height: 100vh;
      display: flex;
      align-items: center;
      background: radial-gradient(circle at center, rgba(20, 20, 20, 0.8), transparent 70%), linear-gradient(135deg, #08192f, #0b1a2d, #060d19);
      color: white;
    }
    
    .hero-title {
      font-size: clamp(2.5rem, 5vw, 4rem);
      font-weight: 800;
      line-height: 1.1;
      background: linear-gradient(to right, var(--primary), var(--secondary));
      -webkit-background-clip: text;
      color: transparent;
      margin-bottom: 1.5rem;
    }
    
    .hero-subtitle {
      font-size: clamp(1rem, 2vw, 1.25rem);
      color: rgba(248, 250, 252, 0.8);
      max-width: 600px;
      line-height: 1.6;
      margin-bottom: 2rem;
    }
    
    .cta-button {
      display: inline-block;
      padding: 1rem 2rem;
      background: linear-gradient(to right, var(--primary), var(--primary-dark));
      color: white;
      font-weight: 600;
      border-radius: 8px;
      text-decoration: none;
      box-shadow: 0 10px 20px -5px rgba(86, 53, 177, 0.4);
      transition: transform 0.3s ease;
    }
    
    .cta-button:hover {
      transform: translateY(-5px);
      box-shadow: 0 15px 30px -5px rgba(34, 106, 201, 0.5);
    }
    
    /* Section Titles */
    .section-title {
      font-size: clamp(1.8rem, 4vw, 2.5rem);
      font-weight: 700;
      margin-bottom: 2rem;
      text-align: center;
      position: relative;
      display: inline-block;
      color: white;
    }
    
    .section-title::after {
      content: '';
      position: absolute;
      bottom: -10px;
      left: 50%;
      transform: translateX(-50%);
      width: 60px;
      height: 4px;
      background: var(--accent);
      border-radius: 2px;
    }
    
    /* Info Cards */
    .data-content {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      gap: 3rem;
      margin-top: 3rem;
    }
    
    .info-card {
      background: rgba(15, 23, 42, 0.8);
      backdrop-filter: blur(12px);
      border: 1px solid rgba(75, 154, 251, 0.2);
      border-radius: 16px;
      padding: 2rem;
      transition: 0.3s ease;
      color: var(--light);
      height: 100%;
      transform-style: preserve-3d;
      transform: perspective(1000px);
    }
    
    .info-card:hover {
      transform: perspective(1000px) translateY(-10px) rotateX(5deg);
      border-color: rgba(75, 154, 251, 0.4);
      box-shadow: 0 20px 40px rgba(0, 0, 0, 0.2),
                  0 0 30px rgba(75, 154, 251, 0.2);
    }
    
    .info-card-icon {
      width: 60px;
      height: 60px;
      background: rgba(28, 6, 230, 0.5);
      border-radius: 12px;
      display: flex;
      align-items: center;
      justify-content: center;
      margin-bottom: 1.5rem;
    }
    
    .info-card-icon svg {
      width: 32px;
      height: 32px;
      fill: var(--primary);
    }
    
    .info-card-title {
      font-size: 1.25rem;
      font-weight: 600;
      margin-bottom: 1rem;
      color: white;
    }
    
    .info-card-text {
      color: rgba(248, 250, 252, 0.9);
      line-height: 1.6;
    }
    
    .highlight-text {
      color: var(--primary);
      font-weight: 500;
    }
    
    /* Pain Points Section */
    .pain-section {
      position: relative;
      overflow: hidden;
      padding: 6rem 1.5rem;
      background: linear-gradient(to bottom,#0a1c3f, #37568b, #0a1c3f);
      color: white;
    }
    
    .pain-section::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: radial-gradient(circle at top right, rgba(75, 154, 251, 0.1), transparent 70%);
      z-index: 0;
    }
    
    .pain-cards {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 2rem;
      margin-top: 3rem;
    }
    
    .pain-card {
      position: relative;
      background: rgba(5, 26, 75, 0.8);
      backdrop-filter: blur(10px);
      border-radius: 16px;
      padding: 2rem;
      overflow: hidden;
      border: 1px solid rgba(75, 154, 251, 0.2);
      transition: all 0.3s ease;
      height: 100%;
      display: flex;
      flex-direction: column;
    }
    
    .pain-card:hover {
      transform: translateY(-10px);
      border-color: rgba(75, 154, 251, 0.4);
      box-shadow: 0 20px 40px rgba(0, 0, 0, 0.2);
    }
    
    .pain-card::before {
      content: '';
      position: absolute;
      top: -50px;
      right: -50px;
      width: 100px;
      height: 100px;
      background: radial-gradient(var(--primary) 0%, transparent 70%);
      opacity: 0.15;
      filter: blur(20px);
      transition: all 0.3s ease;
      z-index: 0;
    }
    
    .pain-card:hover::before {
      opacity: 0.25;
      transform: scale(1.5);
    }
    
    .pain-card-content {
      position: relative;
      z-index: 1;
      flex: 1;
    }
    
    .pain-icon {
      font-size: 2.5rem;
      color: var(--primary);
      margin-bottom: 1rem;
      line-height: 1;
    }
    
    .pain-card-title {
      font-size: 1.25rem;
      font-weight: 600;
      margin-bottom: 1rem;
      color: white;
    }
    
    .pain-card-text {
      color: rgba(248, 250, 252, 0.7);
      font-size: 0.95rem;
      line-height: 1.6;
    }
    
    /* Methods Section */
    .methods-section {
      position: relative;
      overflow: hidden;
      padding: 6rem 1.5rem;
      background: linear-gradient(135deg, var(--dark), #060d19);
      color: white;
    }
    
    .methods-container {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 2rem;
      margin-top: 3rem;
    }
    
    .method-card {
      background: rgba(13, 40, 80, 0.4);
      backdrop-filter: blur(10px);
      border: 1px solid rgba(75, 154, 251, 0.2);
      border-radius: 16px;
      padding: 2rem;
      transition: all 0.3s ease;
      height: 100%;
      transform-style: preserve-3d;
      transform: perspective(1000px);
    }
    
    .method-card:hover {
      transform: perspective(1000px) translateY(-10px) rotateX(5deg);
      border-color: rgba(75, 154, 251, 0.4);
      box-shadow: 0 20px 40px rgba(0, 0, 0, 0.2),
                  0 0 30px rgba(75, 154, 251, 0.2);
    }
    
    .method-icon {
      width: 60px;
      height: 60px;
      display: flex;
      align-items: center;
      justify-content: center;
      margin-bottom: 1.5rem;
      color: var(--primary);
      font-size: 2.5rem;
    }
    
    .method-title {
      font-size: 1.25rem;
      font-weight: 600;
      margin-bottom: 1rem;
      color: white;
    }
    
    .method-text {
      color: rgba(248, 250, 252, 0.9);
      line-height: 1.6;
    }
    
    /* Process Timeline */
    .process-section {
      position: relative;
      padding: 6rem 1.5rem;
    }
    
    .timeline-container {
      position: relative;
      margin-top: 4rem;
    }
    
    .timeline-track {
      position: absolute;
      top: 0;
      left: 50%;
      width: 4px;
      height: 100%;
      background: rgba(75, 154, 251, 0.2);
      transform: translateX(-50%);
      z-index: 1;
    }
    
    .timeline-progress {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 0;
      background: linear-gradient(to bottom, var(--primary), var(--accent));
      transition: height 1.5s ease;
      z-index: 2;
    }
    
    .timeline-items {
      position: relative;
      z-index: 3;
    }
    
    .timeline-item {
      display: flex;
      align-items: center;
      margin-bottom: 6rem;
      opacity: 0;
      transform: translateY(30px);
      transition: all 0.8s ease;
    }
    
    .timeline-item:last-child {
      margin-bottom: 0;
    }
    
    .timeline-item.left {
      flex-direction: row;
    }
    
    .timeline-item.right {
      flex-direction: row-reverse;
    }
    
    .timeline-icon {
      flex: 0 0 120px;
      height: 120px;
      margin: 0 2rem;
      display: flex;
      align-items: center;
      justify-content: center;
      background: rgba(13, 40, 80, 0.5);
      backdrop-filter: blur(10px);
      border: 2px solid rgba(75, 154, 251, 0.3);
      border-radius: 50%;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2), 
                  0 0 20px rgba(75, 154, 251, 0.3);
      position: relative;
      overflow: hidden;
      transition: all 0.4s ease;
      transform-style: preserve-3d;
      transform: perspective(1000px);
    }
    
    .timeline-icon::before {
      content: '';
      position: absolute;
      top: -50%;
      left: -50%;
      width: 200%;
      height: 200%;
      background: radial-gradient(circle at center, rgba(75, 154, 251, 0.2) 0%, transparent 70%);
      opacity: 0;
      transform: scale(0.5);
      transition: all 0.6s ease;
    }
    
    .timeline-icon:hover {
      transform: perspective(1000px) scale(1.05) rotateY(10deg);
      border-color: rgba(75, 154, 251, 0.8);
      box-shadow: 0 15px 40px rgba(0, 0, 0, 0.3), 
                  0 0 30px rgba(75, 154, 251, 0.5);
    }
    
    .timeline-icon:hover::before {
      opacity: 1;
      transform: scale(1);
      animation: pulse 2s infinite;
    }
    
    .timeline-content {
      flex: 1;
      max-width: calc(50% - 4rem);
      padding: 2rem;
      background: rgba(13, 40, 80, 0.4);
      backdrop-filter: blur(10px);
      border-radius: 16px;
      border: 1px solid rgba(75, 154, 251, 0.2);
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
      transition: all 0.4s ease;
      transform-style: preserve-3d;
      transform: perspective(1000px);
    }
    
    .timeline-content:hover {
      transform: perspective(1000px) translateY(-5px) rotateX(5deg);
      border-color: rgba(75, 154, 251, 0.4);
      box-shadow: 0 15px 40px rgba(0, 0, 0, 0.2),
                  0 0 20px rgba(75, 154, 251, 0.2);
    }
    
    .timeline-content h3 {
      font-size: 1.5rem;
      margin-bottom: 1rem;
      color: #ffffff;
      display: flex;
      align-items: center;
    }
    
    .timeline-content h3 span {
      display: flex;
      align-items: center;
      justify-content: center;
      width: 36px;
      height: 36px;
      margin-right: 12px;
      background: linear-gradient(135deg, var(--primary), var(--accent));
      border-radius: 50%;
      font-size: 1.2rem;
    }
    
    .timeline-content p {
      color: rgba(248, 250, 252, 0.7);
      font-size: 1rem;
      line-height: 1.6;
    }
    
    .info-box {
      margin-top: 1.5rem;
      padding: 1rem;
      background: rgba(75, 154, 251, 0.1);
      border-left: 3px solid var(--accent);
      border-radius: 4px;
    }
    
    .info-box h4 {
      font-size: 1.2rem;
      margin-bottom: 0.5rem;
      color: white;
    }
    
    .info-box p {
      font-size: 0.9rem;
      color: rgba(248, 250, 252, 0.7);
    }
    
    /* Case Studies Section */
    .case-studies-section {
      background: linear-gradient(to bottom,#0a1c3f, #37568b, #0a1c3f);
      padding: 6rem 1.5rem;
      position: relative;
      overflow: hidden;
    }
    
    .case-studies-container {
      position: relative;
      z-index: 1;
    }
    
    .case-studies-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
      gap: 2rem;
      margin-top: 3rem;
    }
    
    .case-study-card {
      background: rgba(13, 40, 80, 0.5);
      backdrop-filter: blur(10px);
      border: 1px solid rgba(75, 154, 251, 0.2);
      border-radius: 16px;
      overflow: hidden;
      transition: all 0.3s ease;
    }
    
    .case-study-card:hover {
      transform: translateY(-10px);
      border-color: rgba(75, 154, 251, 0.4);
      box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3),
                  0 0 30px rgba(75, 154, 251, 0.3);
    }
    
    .case-study-image {
      height: 200px;
      background-color: #1a2e50;
      position: relative;
      overflow: hidden;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    
    .case-study-image svg {
      width: 80px;
      height: 80px;
      fill: var(--primary);
      opacity: 0.7;
    }
    
    .case-study-content {
      padding: 1.5rem;
    }
    
    .case-study-tag {
      display: inline-block;
      padding: 0.25rem 0.75rem;
      background: rgba(75, 154, 251, 0.2);
      color: var(--primary);
      border-radius: 50px;
      font-size: 0.8rem;
      margin-bottom: 1rem;
    }
    
    .case-study-title {
      font-size: 1.25rem;
      font-weight: 600;
      margin-bottom: 0.5rem;
      color: white;
    }
    
    .case-study-description {
      color: rgba(248, 250, 252, 0.7);
      font-size: 0.95rem;
      line-height: 1.6;
      margin-bottom: 1.5rem;
    }
    
    .case-study-result {
      display: flex;
      align-items: center;
      padding-top: 1rem;
      border-top: 1px solid rgba(75, 154, 251, 0.2);
    }
    
    .result-icon {
      color: var(--primary);
      margin-right: 0.5rem;
    }
    
    .result-text {
      color: var(--primary);
      font-weight: 600;
    }
    
    /* CTA Section */
    .cta-section {
      text-align: center;
      padding: 6rem 0;
      position: relative;
      overflow: hidden;
    }
    
    .cta-section::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: radial-gradient(circle at center, rgba(75, 154, 251, 0.2), transparent 60%);
      z-index: 0;
    }
    
    .cta-content {
      position: relative;
      z-index: 1;
      max-width: 700px;
      margin: 0 auto;
    }
    
    .cta-title {
      font-size: clamp(1.8rem, 4vw, 2.5rem);
      font-weight: 700;
      margin-bottom: 1.5rem;
      color: white;
    }
    
    .cta-text {
      font-size: 1.1rem;
      color: rgba(248, 250, 252, 0.8);
      margin-bottom: 2.5rem;
    }
    
    .cta-buttons {
      display: flex;
      justify-content: center;
      gap: 1rem;
      flex-wrap: wrap;
    }
    
    .cta-btn-secondary {
      background: transparent;
      border: 2px solid var(--primary);
      color: var(--primary);
    }
    
    /* Animations */
    @keyframes pulse {
      0% {
        opacity: 0.4;
      }
      50% {
        opacity: 0.8;
      }
      100% {
        opacity: 0.4;
      }
    }
    
    @keyframes float {
      0% {
        transform: translateY(0);
      }
      50% {
        transform: translateY(-20px);
      }
      100% {
        transform: translateY(0);
      }
    }
    
    /* Chart visualization */
    .visualization-container {
      height: 300px;
      background-color: rgba(13, 40, 80, 0.5);
      backdrop-filter: blur(10px);
      border-radius: 16px;
      border: 1px solid rgba(75, 154, 251, 0.2);
      margin-top: 3rem;
      overflow: hidden;
      position: relative;
    }
    
    .chart-overlay {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      background: rgba(15, 23, 42, 0.7);
      transition: opacity 0.3s ease;
    }
    
    .chart-text {
      text-align: center;
      max-width: 80%;
    }
    
    .chart-text h3 {
      font-size: 1.5rem;
      color: white;
      margin-bottom: 1rem;
    }
    
    .chart-text p {
      color: rgba(248, 250, 252, 0.8);
      font-size: 1rem;
      margin-bottom: 1.5rem;
    }
    
    .chart-btn {
      display: inline-block;
      padding: 0.5rem 1.5rem;
      background: var(--primary);
      color: white;
      border-radius: 8px;
      font-weight: 500;
      cursor: pointer;
      transition: all 0.3s ease;
    }
    
    .chart-btn:hover {
      background: var(--primary-dark);
      transform: translateY(-3px);
    }
    
    /* Mobile responsiveness */
    @media (max-width: 767px) {
      .section {
        padding: 4rem 1rem;
      }
      
      .timeline-item {
        flex-direction: column !important;
        align-items: flex-start;
        padding-left: 3rem;
        margin-bottom: 4rem;
      }
      
      .timeline-track {
        left: 1.5rem;
      }
      
      .timeline-icon {
        position: absolute;
        left: 0;
        top: 0;
        transform: translateX(-50%);
        width: 60px;
        height: 60px;
        margin: 0;
      }
      
      .timeline-content {
        max-width: 100%;
        margin-top: 2rem;
      }
      
      .pain-card {
        height: auto;
      }
      
      .cta-buttons {
        flex-direction: column;
        align-items: center;
      }
      
      .cta-button {
        width: 100%;
        text-align: center;
        margin-bottom: 1rem;
      }
    }
  </style>
</head>
<body>
  <!-- Hero Section -->
  <section class="hero-section">
    <div class="container">
      <div class="hero-content">
        <h1 class="hero-title">Transforme dados em decisões com análise avançada</h1>
        <p class="hero-subtitle">Extraia insights valiosos de seus experimentos com metodologias estatísticas inovadoras que vão muito além do valor p e transformam seus dados em resultados concretos.</p>
        <a href="#process" class="cta-button">Descubra Nossa Abordagem</a>
      </div>
    </div>
    <div class="absolute top-0 left-0 w-full h-full overflow-hidden">
        <div class="absolute top-1/4 left-1/4 w-96 h-96 blob bg-gradient-to-br from-[#3533c9]/10 to-[#0048b5]/10 blur-3xl animate-float-v"></div>
        <div class="absolute bottom-1/4 right-1/4 w-96 h-96 blob bg-gradient-to-br from-[#0048b5]/10 to-[#4c7fcb]/10 blur-3xl animate-float-h"></div>
        <div class="absolute top-1/2 left-1/2 w-64 h-64 blob bg-gradient-to-br from-[#215ec3]/10 to-transparent blur-2xl animate-pulse-grow"></div>
      </div>
      
      <!-- DNA Animation Overlay -->
      <div class="absolute top-0 left-0 bottow-0 w-full h-full bg-gradient-to-b from-black/50 via-transparent to-black/50 z-12"></div>
        <video autoplay muted loop playsinline class="absolute top-0 left-0 w-full h-full object-cover opacity-15">
        <source src="/video/dna.mp4" type="video/mp4" />
      </video>
  </section>

  
  
  <!-- What is Data Analysis Section -->
  <section id="analysis" class="section methods-section">
    <div class="container">
      <div style="text-align: center; margin-bottom: 3rem;">
        <h2 class="section-title">Nossa abordagem analítica</h2>
        <p style="max-width: 800px; margin: 0 auto; color: rgba(248, 250, 252, 0.8);">Do tradicional ao avançado, utilizamos o método certo para cada desafio</p>
      </div>
      
      <div class="data-content">
        <div class="info-card">
          <div class="info-card-icon">
            <svg viewBox="0 0 24 24" width="32" height="32" fill="#4B9AFB">
              <path d="M4 9h4v11H4zm6-5h4v16h-4zm6 8h4v8h-4z"/>
            </svg>
          </div>
          <h3 class="info-card-title">Estatística tradicional</h3>
          <p class="info-card-text">
            Análises robustas usando métodos estatísticos comprovados como ANOVA, modelos mistos, regressão e testes de hipóteses, mas com uma abordagem que vai <span class="highlight-text">além da simples significância</span> do valor P, focando no tamanho do efeito e relevância prática.
          </p>
        </div>
        
        <div class="info-card">
          <div class="info-card-icon">
            <svg viewBox="0 0 24 24" width="32" height="32" fill="#4B9AFB">
              <path d="M13 7h-2v4H7v2h4v4h2v-4h4v-2h-4V7zm-1-5C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm0 18c-4.41 0-8-3.59-8-8s3.59-8 8-8 8 3.59 8 8-3.59 8-8 8z"/>
            </svg>
          </div>
          <h3 class="info-card-title">Análise multivariada</h3>
          <p class="info-card-text">
            Técnicas avançadas como PCA, PCoA, clustering e Análise discriminante para revelar padrões complexos e relações ocultas nos seus dados que abordagens univariadas não conseguem identificar, fornecendo uma <span class="highlight-text">visão holística</span> dos seus experimentos.
          </p>
        </div>
        
        <div class="info-card">
          <div class="info-card-icon">
            <svg viewBox="0 0 24 24" width="32" height="32" fill="#4B9AFB">
              <path d="M15 9H9v6h6V9zm-2 4h-2v-2h2v2zm8-2V9h-2V7c0-1.1-.9-2-2-2h-2V3h-2v2h-2V3H9v2H7c-1.1 0-2 .9-2 2v2H3v2h2v2H3v2h2v2c0 1.1.9 2 2 2h2v2h2v-2h2v2h2v-2h2c1.1 0 2-.9 2-2v-2h2v-2h-2v-2h2zm-4 6H7V7h10v10z"/>
            </svg>
          </div>
          <h3 class="info-card-title">Machine learning</h3>
          <p class="info-card-text">
            Algoritmos de aprendizado de máquina para prever resultados, classificar fenótipo e identificar fatores determinantes. Nossa abordagem <span class="highlight-text">interpretável</span> garante que você entenda não apenas o que aconteceu, mas também o porquê, transformando modelos complexos em insights aplicáveis.
          </p>
        </div>
      </div>
    </div>
  </section>
  
  <!-- Pain Points Section -->
  <section id="pain-points" class="section pain-section">
    <div class="container">
      <div style="text-align: center; margin-bottom: 3rem;">
        <h2 class="section-title">Superando as dores da pesquisa</h2>
        <p style="max-width: 800px; margin: 0 auto; color: rgba(248, 250, 252, 0.8);">Problemas comuns que impedem o aproveitamento total de seus investimentos em experimentos</p>
      </div>
      
      <div class="pain-cards">
        <div class="pain-card">
          <div class="pain-card-content">
            <div class="pain-icon">⚠️</div>
            <h3 class="pain-card-title">A Tirania do valor P</h3>
            <p class="pain-card-text">
              Quantas vezes experimentos valiosos foram descartados apenas porque não atingiram o famoso P&lt;0,05? A obsessão pelo valor P frequentemente obscurece insights reais e tamanhos de efeito que poderiam ser decisivos para seu negócio. Nosso enfoque vai além dessa métrica única para revelar o verdadeiro potencial dos seus dados.
            </p>
          </div>
        </div>
        
        <div class="pain-card">
          <div class="pain-card-content">
            <div class="pain-icon">💸</div>
            <h3 class="pain-card-title">Alto Investimento, Baixo Retorno</h3>
            <p class="pain-card-text">
              Experimentos dispendiosos com análises rudimentares resultam em conclusões superficiais. Muitas empresas investem recursos significativos na condução de experimentos, mas economizam na análise, perdendo a oportunidade de extrair insights valiosos que justificariam o investimento inicial.
            </p>
          </div>
        </div>
        
        <div class="pain-card">
          <div class="pain-card-content">
            <div class="pain-icon">🧩</div>
            <h3 class="pain-card-title">Complexidade ignorada</h3>
            <p class="pain-card-text">
              Sistemas biológicos e processos industriais são intrinsecamente complexos e interconectados. Análises simplistas frequentemente falham em capturar estas relações, resultando em conclusões parciais ou até mesmo equivocadas que podem direcionar incorretamente suas decisões estratégicas.
            </p>
          </div>
        </div>
        
        <div class="pain-card">
          <div class="pain-card-content">
            <div class="pain-icon">📊</div>
            <h3 class="pain-card-title">Dados sem decisões</h3>
            <p class="pain-card-text">
              Relatórios repletos de tabelas e gráficos, mas escassos em recomendações acionáveis. O abismo entre análises estatísticas e aplicações práticas frequentemente deixa as equipes perdidas sobre como implementar mudanças baseadas nos resultados obtidos.
            </p>
          </div>
        </div>
      </div>
    </div>
  </section>
  
  <!-- Process Section -->
  <section id="process" class="section process-section">
    <div class="container">
      <div style="text-align: center; margin-bottom: 3rem;">
        <h2 class="section-title">Nosso processo analítico</h2>
        <p style="max-width: 800px; margin: 0 auto; color: rgba(248, 250, 252, 0.8);">Uma abordagem estruturada para transformar dados brutos em decisões estratégicas</p>
      </div>
      
      <div class="timeline-container">
        <div class="timeline-track">
          <div class="timeline-progress" id="timelineProgress"></div>
        </div>
        
        <div class="timeline-items">
          <!-- Timeline Item 1 -->
          <div class="timeline-item left" data-step="1">
            <div class="timeline-content">
              <h3><span>1</span>Definição de objetivos</h3>
              <p>
                Iniciamos compreendendo profundamente suas questões de negócio e objetivos científicos. Identificamos as perguntas reais que seu experimento precisa responder, garantindo que toda a análise subsequente seja direcionada para resolver seus desafios específicos.
              </p>
              <div class="info-box">
                <h4>Benefício:</h4>
                <p>Alinhamento entre análise estatística e objetivos estratégicos, maximizando o ROI do experimento.</p>
              </div>
            </div>
            <div class="timeline-icon">
              <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" width="50" height="50" fill="#4B9AFB">
                <path d="M19 3h-4.18C14.4 1.84 13.3 1 12 1c-1.3 0-2.4.84-2.82 2H5c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h14c1.1 0 2-.9 2-2V5c0-1.1-.9-2-2-2zm-7 0c.55 0 1 .45 1 1s-.45 1-1 1-1-.45-1-1 .45-1 1-1zm2 14H7v-2h7v2zm3-4H7v-2h10v2zm0-4H7V7h10v2z"/>
              </svg>
            </div>
          </div>
          
          <!-- Timeline Item 2 -->
          <div class="timeline-item right" data-step="2">
            <div class="timeline-content">
              <h3><span>2</span>Exploração e limpeza de dados</h3>
              <p>
                Realizamos uma análise exploratória completa, identificando padrões preliminares, outliers e relações potenciais. Nossa abordagem rigorosa de pré-processamento garante que os dados estejam adequados para análises avançadas, evitando conclusões enviesadas.
              </p>
              <div class="info-box">
                <h4>Metodologia:</h4>
                <p>Combinação de técnicas de visualização avançada e testes de qualidade de dados para garantir a integridade das análises subsequentes.</p>
              </div>
            </div>
            <div class="timeline-icon">
              <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" width="50" height="50" fill="#4B9AFB">
                <path d="M14.73 13.31c1.13-1.55 1.63-3.58.98-5.74-.68-2.23-2.57-3.98-4.85-4.44-4.65-.93-8.66 3.09-7.73 7.73.46 2.29 2.21 4.18 4.44 4.85 2.16.65 4.19.15 5.74-.98l5.56 5.56c.39.39 1.02.39 1.41 0 .39-.39.39-1.02 0-1.41l-5.55-5.57zM9.5 14C7.01 14 5 11.99 5 9.5S7.01 5 9.5 5 14 7.01 14 9.5 11.99 14 9.5 14z"/>
              </svg>
            </div>
          </div>
          
          <!-- Timeline Item 3 -->
          <div class="timeline-item left" data-step="3">
            <div class="timeline-content">
              <h3><span>3</span>Análise estatística multi-nível</h3>
              <p>
                Aplicamos uma abordagem progressiva, iniciando com estatísticas descritivas, avançando para testes de hipóteses apropriados, e culminando com métodos avançados como análise multivariada e machine learning para revelar insights ocultos nos seus dados.
              </p>
              <div class="info-box">
                <h4>Diferencial:</h4>
                <p>Combinação única de métodos clássicos e inovadores, com interpretação contextualizada para seu segmento específico.</p>
              </div>
            </div>
            <div class="timeline-icon">
              <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" width="50" height="50" fill="#4B9AFB">
                <path d="M19 3H5c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h14c1.1 0 2-.9 2-2V5c0-1.1-.9-2-2-2zM9 17H7v-7h2v7zm4 0h-2V7h2v10zm4 0h-2v-4h2v4z"/>
              </svg>
            </div>
          </div>
          
          <!-- Timeline Item 4 -->
          <div class="timeline-item right" data-step="4">
            <div class="timeline-content">
              <h3><span>4</span>Tradução para ações concretas</h3>
              <p>
                Transformamos resultados estatísticos em recomendações práticas e acionáveis. Nossa equipe multidisciplinar garante que cada insight seja contextualizado dentro da realidade do seu negócio, facilitando a implementação de melhorias tangíveis baseadas em evidências.
              </p>
              <div class="info-box">
                <h4>Resultado:</h4>
                <p>Relatório executivo com visualizações intuitivas e recomendações priorizadas para maximizar o retorno sobre seus investimentos em pesquisa.</p>
              </div>
            </div>
            <div class="timeline-icon">
              <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" width="50" height="50" fill="#4B9AFB">
                <path d="M9 21c0 .55.45 1 1 1h4c.55 0 1-.45 1-1v-1H9v1zm3-19C8.14 2 5 5.14 5 9c0 2.38 1.19 4.47 3 5.74V17c0 .55.45 1 1 1h6c.55 0 1-.45 1-1v-2.26c1.81-1.27 3-3.36 3-5.74 0-3.86-3.14-7-7-7zm2.85 11.1l-.85.6V16h-4v-2.3l-.85-.6C7.8 12.16 7 10.63 7 9c0-2.76 2.24-5 5-5s5 2.24 5 5c0 1.63-.8 3.16-2.15 4.1z"/>
              </svg>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>
  
  <!-- Case Studies Section -->
  <section id="case-studies" class="section case-studies-section">
    <div class="container case-studies-container">
      <div style="text-align: center; margin-bottom: 3rem;">
        <h2 class="section-title">Casos de Sucesso</h2>
        <p style="max-width: 800px; margin: 0 auto; color: rgba(248, 250, 252, 0.8);">Transformando dados em resultados concretos para diversos setores</p>
      </div>
      
      <div class="case-studies-grid">
        <!-- Case Study 1 -->
        <div class="case-study-card">
          <div class="case-study-image">
            <svg viewBox="0 0 24 24">
              <path d="M20 2H4c-1.1 0-2 .9-2 2v16c0 1.1.9 2 2 2h16c1.1 0 2-.9 2-2V4c0-1.1-.9-2-2-2zm-9 13h2V7h-2v8z"/>
            </svg>
          </div>
          <div class="case-study-content">
            <span class="case-study-tag">Indústria Pet</span>
            <h3 class="case-study-title">Otimização de Palatabilidade</h3>
            <p class="case-study-description">
              Uma empresa líder em alimentos pet enfrentava desafios na consistência de resultados de palatabilidade. Nossa análise multivariada identificou interações ocultas entre ingredientes que métodos tradicionais haviam ignorado.
            </p>
            <div class="case-study-result">
              <span class="result-icon">✓</span>
              <span class="result-text">Aumento de 28% na preferência em testes dupla escolha</span>
            </div>
          </div>
        </div>
        
        <!-- Case Study 2 -->
        <div class="case-study-card">
          <div class="case-study-image">
            <svg viewBox="0 0 24 24">
              <path d="M12 5.9c1.16 0 2.1.94 2.1 2.1s-.94 2.1-2.1 2.1S9.9 9.16 9.9 8s.94-2.1 2.1-2.1m0 9c2.97 0 6.1 1.46 6.1 2.1v1.1H5.9V17c0-.64 3.13-2.1 6.1-2.1M12 4C9.79 4 8 5.79 8 8s1.79 4 4 4 4-1.79 4-4-1.79-4-4-4zm0 9c-2.67 0-8 1.34-8 4v3h16v-3c0-2.66-5.33-4-8-4z"/>
            </svg>
          </div>
          <div class="case-study-content">
            <span class="case-study-tag">Suinocultura</span>
            <h3 class="case-study-title">Análise Integrada de Desempenho</h3>
            <p class="case-study-description">
              Uma empresa de aditivos tinha múltiplos conjuntos de dados desconexos (Desempenho, imunologia, variáveis ambientais). Nossos modelos de machine learning unificaram estas fontes, revelando os principais fatores de conversão alimentar.
            </p>
            <div class="case-study-result">
              <span class="result-icon">✓</span>
              <span class="result-text">Melhora de 5,2% na conversão alimentar</span>
            </div>
          </div>
        </div>
        
        <!-- Case Study 3 -->
        <div class="case-study-card">
          <div class="case-study-image">
            <svg viewBox="0 0 24 24">
              <path d="M19 3H5c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h14c1.1 0 2-.9 2-2V5c0-1.1-.9-2-2-2zm0 16H5V5h14v14zm-4-4h-4v-2h4v-2h-4V9h4V7H9v10h6z"/>
            </svg>
          </div>
          <div class="case-study-content">
            <span class="case-study-tag">Farmacêutica</span>
            <h3 class="case-study-title">Eficácia de Medicamento Veterinário</h3>
            <p class="case-study-description">
              Testes iniciais de um novo medicamento apresentaram resultados inconclusivos. Nossa reanálise utilizando modelos bayesianos e análise de subgrupos identificou perfis específicos de animais com resposta significativa.
            </p>
            <div class="case-study-result">
              <span class="result-icon">✓</span>
              <span class="result-text">Adaptação do produto para segmentos específicos</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>
  
  
  <!-- CTA Section -->
  <section class="section cta-section">
    <div class="container">
      <div class="cta-content">
        <h2 class="cta-title">Maximize o Retorno dos Seus Experimentos</h2>
        <p class="cta-text">
          Não se contente com análises superficiais. Descubra o potencial oculto nos seus dados e transforme-os em vantagem competitiva com nossa abordagem analítica.
        </p>
        <div class="cta-buttons">
          <a href="#contato" class="cta-button">Agendar Consultoria</a>
        </div>
      </div>
    </div>
  </section>
  
  <!-- Simplified JavaScript for better performance -->
  <script>
    document.addEventListener('DOMContentLoaded', () => {
      // Create animated particles for visual interest
      function createParticles() {
        const container = document.querySelector('.hero-section');
        if (!container) return;
        
        const particleCount = 20;
        
        for (let i = 0; i < particleCount; i++) {
          const particle = document.createElement('div');
          particle.style.position = 'absolute';
          particle.style.borderRadius = '50%';
          particle.style.background = 'rgba(75, 154, 251, 0.2)';
          particle.style.boxShadow = '0 0 10px rgba(75, 154, 251, 0.3)';
          
          // Random size
          const size = Math.random() * 15 + 5;
          particle.style.width = `${size}px`;
          particle.style.height = `${size}px`;
          
          // Random position
          const posX = Math.random() * 100;
          const posY = Math.random() * 100;
          particle.style.left = `${posX}%`;
          particle.style.top = `${posY}%`;
          
          // Add some animation
          particle.style.transition = 'transform 3s ease-in-out, opacity 3s ease-in-out';
          particle.style.animation = `float ${Math.random() * 10 + 10}s infinite ease-in-out`;
          particle.style.animationDelay = `${Math.random() * 5}s`;
          
          container.appendChild(particle);
        }
      }
      
      createParticles();
      
      // Timeline animation with simple JavaScript
      const timelineItems = document.querySelectorAll('.timeline-item');
      const timelineProgress = document.getElementById('timelineProgress');
      
      // Function to check if an element is in viewport
      function isElementInViewport(el) {
        const rect = el.getBoundingClientRect();
        return (
          rect.top >= 0 &&
          rect.left >= 0 &&
          rect.bottom <= (window.innerHeight || document.documentElement.clientHeight) &&
          rect.right <= (window.innerWidth || document.documentElement.clientWidth)
        );
      }
      
      // Function to animate elements when they come into view
      function animateOnScroll() {
        let progressStep = 0;
        
        timelineItems.forEach((item, index) => {
          if (isElementInViewport(item)) {
            item.style.opacity = '1';
            item.style.transform = 'translateY(0)';
            
            // Update progress step if this is the furthest item seen
            if (index + 1 > progressStep) {
              progressStep = index + 1;
            }
          }
        });
        
        // Update timeline progress
        if (timelineProgress) {
          const progress = (progressStep / timelineItems.length) * 100;
          timelineProgress.style.height = `${progress}%`;
        }
      }
      
      // Animate elements in viewport on page load
      animateOnScroll();
      
      // Animate elements when scrolling
      window.addEventListener('scroll', animateOnScroll);
      
      // Chart overlay interaction
      const chartBtn = document.querySelector('.chart-btn');
      const chartOverlay = document.querySelector('.chart-overlay');
      
      if (chartBtn && chartOverlay) {
        chartBtn.addEventListener('click', () => {
          chartOverlay.style.opacity = '0';
          setTimeout(() => {
            chartOverlay.style.display = 'none';
          }, 300);
        });
      }
      
      // Add animation keyframes to the document
      const styleSheet = document.createElement('style');
      styleSheet.textContent = `
        @keyframes float {
          0% { transform: translateY(0); }
          50% { transform: translateY(-20px); }
          100% { transform: translateY(0); }
        }
        
        @keyframes pulse {
          0% { opacity: 0.4; }
          50% { opacity: 0.8; }
          100% { opacity: 0.4; }
        }
      `;
      document.head.appendChild(styleSheet);
    });
  </script>
 </body>
  </html>

 <Footer />
</Layout>